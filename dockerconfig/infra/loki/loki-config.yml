# Loki — mode monolithic (single-binary), storage filesystem.
# Cukup untuk dev & deployment kecil. Hanya diakses gateway & Alloy (internal).

auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096
  log_level: warn

common:
  instance_addr: 127.0.0.1
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

limits_config:
  # Izinkan ingest log bertanggal lampau — fitur linkDate men-trigger Alloy
  # membaca file lama yang timestamp-nya jauh di belakang.
  reject_old_samples: false
  retention_period: 2160h # 90 hari (lihat LOG_RETENTION_DAYS)
  allow_structured_metadata: true
  volume_enabled: true

compactor:
  working_directory: /loki/compactor
  retention_enabled: true
  delete_request_store: filesystem

ruler:
  storage:
    type: local
    local:
      directory: /loki/rules

analytics:
  reporting_enabled: false
